# EN ⇄ ID Translation Review

Scope: `src/locales/{en,id}/*.json` (i18next-style namespaces) and `src/ce-ui/locale/index.tsx` (shared UI-kit locale object).

Generated by flattening both locale trees and diffing key-by-key. "Untranslated" below means the Indonesian value is **byte-identical** to the English value — it does not necessarily mean it's wrong (brand names, API terms, and abbreviations are legitimately identical in both languages). Flagged for your review, not auto-fixed.

---

## 1. `src/locales/en/*.json` vs `src/locales/id/*.json`

| Namespace | Total keys | Identical EN/ID (flagged) | Structural mismatches |
|---|---|---|---|
| auth.json | 90 | 12 | none |
| common.json | 14 | 0 | none |
| dashboard.json | 113 (en) / 114 (id) | 15 | 1 key exists only in `id` |
| delivery.json | 85 | 3 | none |
| website.json | 238 | 22 | none |

**Total flagged for review: 52 keys** across 5 namespaces.

### auth.json — 12 identical values

| Key | EN = ID value | Likely OK as-is? |
|---|---|---|
| `loginFooter.malformedSSOButton` | `Malformed SSO` | needs translation |
| `loginFooter.mismatchButton` | `Sync & Entry Mismatch` | needs translation |
| `onboarding.basic.labamuApp` | `Labamu App` | brand name — OK |
| `onboarding.basic.languages.english` | `English` | proper noun — OK |
| `onboarding.basic.languages.indonesian` | `Bahasa Indonesia` | proper noun — OK |
| `onboarding.basic.mrp` | `MRP` | acronym — OK |
| `onboarding.header.brand` | `ShopFront` | brand name — OK |
| `onboarding.header.user` | `Natasha Smith` | person name — OK |
| `onboarding.steps.styling` | `Styling` | needs translation |
| `onboarding.styling.banner.title` | `Banner (0/3)` | needs translation |
| `onboarding.styling.font.title` | `Font` | needs translation (or OK, loanword) |
| `onboarding.styling.title` | `Styling` | needs translation |

### common.json — 0 identical values
No issues found.

### dashboard.json — 15 identical values + 1 structural mismatch

| Key | EN = ID value | Likely OK as-is? |
|---|---|---|
| `header.title` | `Labamu Ecommerce` | brand name — OK |
| `profile.activities.both` | `Online & Offline` | needs translation |
| `profile.activities.offline` | `Offline` | loanword — OK |
| `profile.activities.online` | `Online` | loanword — OK |
| `profile.fields.rtRw` | `RT/RW` | Indonesian admin term — OK |
| `profile.industries.fashion` | `Fashion` | needs translation (or loanword — OK) |
| `profile.industries.nonprofit` | `Non-Profit / NGO` | needs translation |
| `profile.industries.real_estate_general` | `Real Estate` | needs translation |
| `profile.industries.tour_travel` | `Tour & Travel` | needs translation (or loanword — OK) |
| `profile.membership.basic` | `Basic Member` | needs translation |
| `profile.membership.pro` | `Pro Member` | needs translation |
| `sidebar.dashboard` | `Dashboard` | needs translation (or loanword — OK) |
| `sidebar.modifier` | `Modifier` | needs translation |
| `sidebar.websites` | `Website Studio` | needs translation |
| `welcome.messageStrong` | `Labamu Ecommerce` | brand name — OK |

**Structural mismatch:** `profile.savingBtn` (value `Menyimpan...`) exists only in `id/dashboard.json`. There is no corresponding key in `en/dashboard.json` — needs to be added on the English side (likely `Saving...`) or the key removed if unused.

### delivery.json — 3 identical values

| Key | EN = ID value | Likely OK as-is? |
|---|---|---|
| `courier.apiKey` | `API Key` | technical term — OK |
| `courier.apiSecret` | `API Secret` | technical term — OK |
| `courier.stepByStep.step1.item1LinkText` | `Lalamove Partner Portal` | proper noun — OK |

### website.json — 22 identical values

| Key | EN = ID value | Likely OK as-is? |
|---|---|---|
| `gallery.edit` | `Edit Template` | needs translation |
| `gallery.templates.barger.title` | `Barger Joint` | template name — OK |
| `gallery.templates.dekor.title` | `DEKOR` | template name — OK |
| `gallery.templates.houzez.title` | `Houzez` | template name — OK |
| `gallery.templates.local.title` | `Local Produce` | template name — OK |
| `gallery.templates.medic.title` | `Medic` | template name — OK |
| `gallery.templates.napoli.title` | `Napoli` | template name — OK |
| `gallery.templates.photostoodio.title` | `Photo Stoodio` | template name — OK |
| `gallery.templates.xinear.title` | `Xinear Pro` | template name — OK |
| `studio.basic.nav.heroLabel` | `Headline` | needs translation |
| `studio.configure.panels.footer` | `Footer` | needs translation |
| `studio.configure.panels.header` | `Header & Hero` | needs translation |
| `studio.editor.banner` | `Banner` | needs translation (or loanword — OK) |
| `studio.editor.hero` | `Hero` | needs translation |
| `studio.editor.logo` | `Logo` | loanword — OK |
| `studio.header.breadcrumbStudio` | `Studio` | needs translation (or loanword — OK) |
| `studio.publish.subdomain.suffix` | `.labamu.store` | domain suffix — OK |
| `studio.title` | `Studio` | needs translation (or loanword — OK) |
| `template_houzez.footer.address` | full street address | address — OK |
| `template_houzez.footer.email` | `houzez@gmail.com` | email — OK |
| `template_houzez.footer.location` | `Tangerang` | place name — OK |
| `template_houzez.map.address` | same street address | address — OK |

---

## 2. `src/ce-ui/locale/index.tsx` — `idLocale` vs `enLocale`

This file is a hand-written TS object (not JSON), fully translated for nearly every field. Only one identical value found:

| Key | EN = ID value | Likely OK as-is? |
|---|---|---|
| `table.totalData` | `${total} total data` (function, line 103 / 185) | phrase is identical in both — needs translation (e.g. ID: `${total} total data` → consider `${total} data`) |

`amLabel`/`pmLabel` (`AM`/`PM`) are also identical between locales but are standard abbreviations left as-is intentionally — not flagged as an issue.

Everything else in this file (loading, table, dropdown, filterPill, breadcrumbs, textField, datePicker, dateField, timePicker, otpPopup, imageCrop, phoneField) has distinct, translated ID values.

---

## Suggested next steps
1. Translate the "needs translation" rows above (~30 keys) in `auth.json`, `dashboard.json`, and `website.json`.
2. Add the missing `profile.savingBtn` key to `en/dashboard.json` (or remove from `id/dashboard.json` if it's dead).
3. Fix `table.totalData` in `src/ce-ui/locale/index.tsx` ID copy.
4. Rows marked "OK" (brand names, proper nouns, loanwords, technical terms, addresses) can likely stay as-is — spot-check only.
